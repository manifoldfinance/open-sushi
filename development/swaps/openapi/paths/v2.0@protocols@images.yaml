get:
  operationId: getImages
  tags:
    - Protocols
  description: All supported protocols images
  responses:
    '200':
      description: Success
      content:
        application/json:
          schema:
            $ref: ../components/schemas/ProtocolsImages.yaml
    '500':
      description: Internal Server Error
